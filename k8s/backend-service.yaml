apiVersion: v1
kind: Service
metadata:
  name: jlox-backend-service
  labels:
    app: jlox-backend
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: jlox-backend

